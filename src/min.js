
/**
 *  Returns the smaller argument.
 *
 * @param {*} a
 * @param {*} b
 * @return {*}
 * @example
 *
 *  min(8765, 1224) // 1224
 *  min('a', 'b'); // 'a'
 *
 */

const min = (a, b) => b < a ? b : a

export default min
