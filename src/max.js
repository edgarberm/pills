
/**
 *  Returns the larger argument.
 *
 * @param {*} a
 * @param {*} b
 * @return {*}
 * @example
 *
 *  max(8765, 1224) // 8765
 *  max('a', 'b'); // 'b'
 *
 */

const max = (a, b) => b > a ? b : a

export default max
